# 隐私说明

简体中文 | [English](PRIVACY.md)

本文描述官方 Zinc 源码树及其构建产物的数据边界；修改版发行物可能有不同表现。

## 本机保存的数据

Zinc 在 Electron 的当前用户 `userData` 目录中保存：

- 应用设置，包括已配置的 Codex 会话根目录；
- 标签顺序、工作目录等会话恢复元数据；
- 粘贴到终端的剪贴板图片（`PastedImages` 目录）。

Zinc 会把粘贴图片的本机绝对路径输入到当前终端。每次启动时，Zinc 会尝试只删除自身
`PastedImages` 目录中超过 30 天的普通文件；如果 Zinc 没有启动或文件不可访问，清理
会延后。卸载可能保留整个用户数据目录，以便重新安装后继续使用原设置。

这些文件可能暴露私人路径、shell 选择、图片和工作内容，不应提交到源码仓库或公开
Issue。

## 可选的 AI 用量状态

启用 AI 用量状态栏后，Zinc 会在本机检查当前终端标签页，可能进行以下操作：

- 枚举后代进程的 PID、父 PID 和可执行文件名，并临时读取后代进程命令行，直到识别
  出 Codex 或 Claude；
- 读取已配置的本机或 WSL Codex 根目录中近期会话 `.jsonl` 文件的尾部（最多
  256 KiB），以及 `config.toml`；
- 读取操作系统临时目录中的 Claude 状态行文件 `cc_status.json`、`cc_daily.txt` 和
  `cc_weekly.txt`。

对于 Codex，Zinc 只提取状态栏所需的模型、推理强度、令牌数、用量百分比和重置时间，
不会显示或持久化提示词、回复或其他原始 JSONL 内容。对于 Claude，Zinc 从 JSON 文件
中提取模型、推理强度、上下文令牌数、用量百分比和重置时间，并显示每日及每周费用
文本。这些 Claude 文件是用户的 Claude/状态行配置所生成的可选本机输出，不属于
Zinc 数据；Zinc 不会创建或管理它们。

原始命令行和源文件内容不会发送到渲染进程、由 Zinc 保存或上传。解析后的状态值在
Zinc 运行期间可能暂存在有上限的 worker 内存中。状态错误只向标准错误输出通用分类，
Zinc 不会写入 AI 状态错误文件。不过，命令行、JSONL 和第三方状态文件本身可能包含
敏感信息；能够读取终端或进程内存的其他本机软件仍属于用户需要管理的本机安全边界。

启动时，Zinc 可能查询已安装的 WSL 发行版名称及各发行版默认用户的 `$HOME`，以发现
该用户的 `.codex` 根目录；它不会枚举所有 `/home` 目录。关闭“显示 AI 用量状态栏”会
停止周期性的进程检查和用量文件读取，但启动时的 WSL 根目录发现仍可能执行。取消勾选
单个工具后，Zinc 在检测完成后不会读取该工具的用量文件。

## 网络活动

Zinc 不包含分析统计、广告遥测或用户账户服务。打包版只在用户检查或下载更新时连接
GitHub Releases。网页链接经过协议校验后交给系统浏览器打开。AI 状态检查完全在本机
进行，Zinc 不会上传检查结果。

用户在终端中运行的命令、shell、开发工具和子进程属于独立程序，可能按照各自配置与
隐私政策访问网络。输入这些程序的路径或数值随后由相应程序处理，并非由 Zinc 的网络
代码处理。

## 诊断信息

Zinc 不会自动上传终端输出、命令历史、设置、会话状态、截图、状态源文件内容或崩溃
转储。如果用户主动把这些内容发到 GitHub，则适用 GitHub 的隐私条款，内容也可能公开。

分享前请删除用户名、本地/网络路径、命令历史、进程命令行、环境变量、令牌、Cookie、
主机名、IP、仓库名、图片和文档内容。优先提供最小文字复现，而不是完整截图或设置文件。

## Windows Terminal 设置示例

仓库只保留人工编写的示例。开发者可在本机生成真实快照用于测试，但该文件会被 Git
忽略，禁止提交。

## 你的选择

- 不希望保存工作目录时可关闭会话恢复。
- 不希望 Zinc 在本机检查进程和用量文件时，可关闭 AI 用量状态栏；第三方状态文件需
  另行保护或删除。
- 不再需要时删除粘贴图片与 Zinc 本机用户数据目录；需要立即删除时不要只依赖启动清理。
- 不希望连接 GitHub Releases 时不要执行更新检查。
- 分别检查终端子进程自身的隐私设置。

隐私敏感问题请按照 [`SECURITY.zh-CN.md`](SECURITY.zh-CN.md) 私下报告。
